<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFFFFF"> <!-- Adjust the background color to match your app theme -->

    <!-- TextView for 'Let's Start Screening' -->
    <TextView
        android:id="@+id/tvStartScreening"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Let's Start Screening"
        android:textSize="30sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="true"
        android:textColor="#000000" /> <!-- Adjust the text color to match your app theme -->

    <!-- ImageView for a large orange arrow -->
    <ImageView
        android:id="@+id/imgArrow"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_below="@id/tvStartScreening"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:src="@drawable/baseline_double_arrow_24" />

</RelativeLayout>
